@model FlightApp.ViewModels.ShoppingCartVM

@{
    ViewData["Title"] = "Your Basket";
}

<h1>Your Basket</h1>

@if (TempData["Message"] != null)
{
    <div class="alert alert-info">@TempData["Message"]</div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

@if (!Model.RouteItems.Any())
{
    <div class="alert alert-warning">Your basket is empty.</div>
    <p>
        <a asp-controller="Flights" asp-action="GetRoutes" class="btn btn-primary">Search for Routes</a>
    </p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Amount</th>
                <th>FlightId</th>
                <th>Departure Time</th>
                <th>Departure City</th>
                <th>Arrival City</th>
                <th>Arrival Time</th>
                <th>Price</th>
                <th>Booking Class</th>
                <th>Remove from cart</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.RouteItems)
            {
                @foreach (var flight in item.Flights)
                {
                    <tr>
                        <td>@item.PassengerCount</td>
                        <td>@flight.FlightId</td>
                        <td>@flight.DepartureTime?.ToString("dd/MM/yyyy HH:mm")</td>
                        <td>@flight.DepartureCity</td>
                        <td>@flight.ArrivalCity</td>
                        <td>@flight.ArrivalTime?.ToString("dd/MM/yyyy HH:mm")</td>
                        <td>
                            @{
                                // Calculate adjusted price based on booking class
                                double adjustedPrice = 0;

                                if (item.Passengers?.Any() == true)
                                {
                                    // Use the first passenger's booking class for display
                                    var firstPassenger = item.Passengers.First();
                                    adjustedPrice = (flight.Price ?? 0) * firstPassenger.BookingClassPriceFactor * item.PassengerCount;
                                }
                                else
                                {
                                    adjustedPrice = (flight.Price ?? 0) * item.PassengerCount;
                                }
                            }
                            @adjustedPrice.ToString("C")
                        </td>
                        <td>
                            @if (item.Passengers?.Any() == true)
                            {
                                // Just show the booking class name (assuming all passengers have the same class)
                                @item.Passengers.First().BookingClassName
                            }
                            else
                            {
                                <span>Not specified</span>
                            }
                        </td>
                        <td>
                            <form asp-action="RemoveFlightFromCart" asp-controller="ShoppingCart" method="post">
                                <input type="hidden" name="routeId" value="@item.RouteId" />
                                <input type="hidden" name="flightId" value="@flight.FlightId" />
                                <button type="submit" class="btn btn-sm btn-danger">Remove</button>
                            </form>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>

    <div class="text-right">
        <h4>Total: @Model.ComputeTotalValue().ToString("C")</h4>
    </div>

    <div class="mt-3">
        <a href="#" class="btn btn-success">Proceed to Checkout</a>
        <a asp-controller="Flights" asp-action="GetRoutes" class="btn btn-secondary">Continue Shopping</a>
    </div>
}